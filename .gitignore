# Go
bin/
*.test
*.out

# Frontend
web/node_modules/
web/dist/
web/.vite/

# Runtime state — never commit real config or the database
data/
config.yaml
*.db
*.db-*

# dross machine-local state
.dross/state.json
.dross/local.toml

# OS / editor
.DS_Store
.idea/
.vscode/

# Stray root build output (`go build ./...`)
/adguard-reward

# mutation-tool raw reports (dross verify regenerates these)
reports/

# dross handoff — local working memory, not tracked
.dross/handoff.md
